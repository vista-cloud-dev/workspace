# vista-cloud-dev org — clone-all manifest.
# One repo per line; blank lines and '#' comments are ignored.
# Branch overrides (for repos not worked on from main) live in bootstrap.sh.
# Grouped by the org m/v scope split (see CLAUDE.md); order is not significant.

# --- coordination / docs / templates ---
workspace
docs
doc-framework
go-cli-template

# --- m-* : engine-neutral M toolchain (runs on a bare M engine, no VistA) ---
m-cli
m-stdlib
m-parse
m-driver-sdk
m-ydb
m-iris
m-dev-tools-mcp

# --- v-* : VistA-specific (needs Kernel/FileMan/KIDS) ---
v-stdlib
v-pkg
v-cli
v-web
vpng
vista-iris
vista-info-hub

# --- vdocs (PRIVATE — clone needs org access) ---
vdocs-cli
vdocs-tui
vdocs-web
